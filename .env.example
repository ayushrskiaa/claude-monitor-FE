# Claude Monitor Frontend — Environment Variables
# Copy to .env.local and fill in your values

# Optional: Pre-configure the server URL (users can override in dashboard)
NEXT_PUBLIC_SERVER_URL=http://localhost:3000
